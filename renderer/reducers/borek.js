const initState = {
  borek: "cokertmeden ciktim da halilim"
};

const borek = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default borek;
